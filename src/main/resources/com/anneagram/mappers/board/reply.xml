<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="Reply">
	
	<insert id="r_insert" parameterType="r">
		insert into reply (rno, bno, user_id, nickname , rcontent, regdate) 
		values (rno_seq.nextval, #{bno}, #{user_id}, #{nickname} ,#{rcontent}, sysdate)
	</insert>
	
	<insert id="r_insert_no" parameterType="r">
		insert into reply (rno, bno, rcontent, regdate, no_user_id, no_user_passwd) 
		values (rno_seq.nextval, #{bno}, #{rcontent}, sysdate, #{no_user_id}, #{no_user_passwd})
	</insert>

	<select id="r_list" resultType="r" >
		select * from reply where bno=#{bno}
	</select>
</mapper>